$(document).bind("keydown", function(event) {
    if (event.altKey && event.code === "KeyC")
    {
        window.location.replace("/vitco-india/control/complaints");
    }
    else if (event.altKey && event.code === "KeyS")
    {
        window.location.replace("/vitco-india/control/complaints");
    }

});